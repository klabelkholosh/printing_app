{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('machine_index') }}" class="btn btn-default">Back to the list</a>
    <h1 class="page-header">Machine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ machine.description }}</td>
            </tr>
            <tr>
                <th>Hourlyrate</th>
                <td>{{ machine.hourlyrate }}</td>
            </tr>
            <tr>
                <th>Machinecode</th>
                <td>{{ machine.machinecode }}</td>
            </tr>
        </tbody>
    </table>

    
   
        
            <a href="{{ path('machine_edit', { 'machinecode': machine.machinecode }) }}" class="btn btn-primary">Edit</a>
    
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
     
{% endblock %}
