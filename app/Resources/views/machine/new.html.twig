{% extends 'base.html.twig' %}

{% block body %}
   <a href="{{ path('machine_index') }}" class="btn btn-default">Back to the list</a>
    <h1 class="page-header">Machine creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-danger"/>
    {{ form_end(form) }}

    
            

{% endblock %}
